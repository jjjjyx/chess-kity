(function() {
    window.CHESS_CONFIG = {
        // 定义
        //modules: [],
        // 只读模式，默认是false
        readOnly: false,

        // 默认主题
        defaultTemplate: 'stype1',

        // 语言，默认是 'zh-cn'
        lang: 'zh-cn',
    };
})();